"""Example custom point loader for project-specific lidar sources."""

from __future__ import annotations

from typing import Any


def load_points(lidar_source: Any, at720: bool = False):
    """Return (points, valid_mask).

    `points` should be an `NxC` float array with at least xyz in the first 3 columns.
    `valid_mask` should be a boolean mask aligned with the raw point array before any
    downstream filtering.
    """
    raise NotImplementedError(
        "Implement this file for your own data source, then rename it to "
        "`custom_point_loader.py`."
    )
